"""Capability catalog, parsing and runtime loading helpers.

Submodules are intentionally not imported here so lightweight consumers such
as the Skill parser do not require database dependencies at import time.
"""
